import { Routes } from '@angular/router';
import { HandlerTopicStatusComponent } from './handler-topic-status.component';

export default [
    {
        path     : '',
        component: HandlerTopicStatusComponent
    }
] as Routes;
